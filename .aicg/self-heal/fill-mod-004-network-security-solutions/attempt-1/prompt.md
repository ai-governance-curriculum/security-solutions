# Self-heal: address verify findings for fill-mod-004-network-security-solutions

## Goal

The previous attempt at this work item produced content that
failed contract verification. Fix the specific findings listed
below by editing only the affected files. Do NOT regenerate
from scratch and do NOT broaden the scope.

## Findings

### 1. `missing_required_sections` (error)

- Target: `?`
- Message: modules/mod-004-network-security/exercise-01-cni-evaluation/SOLUTION.md is missing required heading(s): implementation.
- missing: ['implementation']
- seen: ['1. solution overview', '2. worked answer', '2.1 candidate set', '2.2 scoring rubric (1–5 per criterion)', '2.3 worked recommendation example', '2.4 what changes the recommendation', '3. validation steps', '4. rubric / review checklist', '5. common mistakes', '6. references', 'solution — exercise 01: cni evaluation']

### 2. `needs_research_marker` (error)

- Target: `?`
- Message: modules/mod-004-network-security/exercise-01-cni-evaluation/SOLUTION.md still contains a `needs-research` marker.

### 3. `needs_research_marker` (error)

- Target: `?`
- Message: modules/mod-004-network-security/exercise-03-edge-gateway-hardening/SOLUTION.md still contains a `needs-research` marker.

### 4. `missing_required_sections` (error)

- Target: `?`
- Message: modules/mod-004-network-security/exercise-04-rate-limit-ddos-design/SOLUTION.md is missing required heading(s): implementation.
- missing: ['implementation']
- seen: ['1. solution overview', '2. worked design', '2.1 traffic taxonomy', '2.2 l3/l4 edge controls', '2.3 l7 token buckets at the gateway', '2.4 concurrency, queue, and body limits', '2.5 per-workload admission control (inference server)', '2.6 failure modes', '2.7 headers returned to callers', '2.8 observability hooks for exercise 05', '3. validation steps', '3.1 functional checks', '3.2 failure-mode checks (game day)', '3.3 headers and signalling', '4. rubric / review checklist', '5. common mistakes', '6. references', 'solution — exercise 04: rate-limit and ddos design']

### 5. `needs_research_marker` (error)

- Target: `?`
- Message: modules/mod-004-network-security/exercise-04-rate-limit-ddos-design/SOLUTION.md still contains a `needs-research` marker.

### 6. `missing_required_sections` (error)

- Target: `?`
- Message: modules/mod-004-network-security/exercise-05-network-observability-plan/SOLUTION.md is missing required heading(s): implementation.
- missing: ['implementation']
- seen: ['1. solution overview', '2. worked plan', '2.1 layered telemetry sources', '2.2 schema (canonical fields)', '2.3 pipeline', '2.4 detection set (illustrative)', '2.5 dashboards (grafana / equivalent)', '2.6 retention and access', '2.7 privacy and least-disclosure', '2.8 health of the observability pipeline itself', '3. validation steps', '3.1 telemetry presence', '3.2 synthetic detection inputs', '3.3 privacy review checklist', '3.4 tabletop', '4. rubric / review checklist', '5. common mistakes', '6. references', 'solution — exercise 05: network observability plan']

### 7. `needs_research_marker` (error)

- Target: `?`
- Message: modules/mod-004-network-security/exercise-05-network-observability-plan/SOLUTION.md still contains a `needs-research` marker.

### 8. `missing_required_sections` (error)

- Target: `modules/mod-004-network-security/exercise-01-cni-evaluation/SOLUTION.md`
- Message: modules/mod-004-network-security/exercise-01-cni-evaluation/SOLUTION.md is missing required heading(s): implementation.
- missing: ['implementation']
- seen: ['1. solution overview', '2. worked answer', '2.1 candidate set', '2.2 scoring rubric (1–5 per criterion)', '2.3 worked recommendation example', '2.4 what changes the recommendation', '3. validation steps', '4. rubric / review checklist', '5. common mistakes', '6. references', 'solution — exercise 01: cni evaluation']

### 9. `needs_research_marker` (error)

- Target: `modules/mod-004-network-security/exercise-01-cni-evaluation/SOLUTION.md`
- Message: modules/mod-004-network-security/exercise-01-cni-evaluation/SOLUTION.md still contains a `needs-research` marker.

### 10. `needs_research_marker` (error)

- Target: `modules/mod-004-network-security/exercise-03-edge-gateway-hardening/SOLUTION.md`
- Message: modules/mod-004-network-security/exercise-03-edge-gateway-hardening/SOLUTION.md still contains a `needs-research` marker.

### 11. `missing_required_sections` (error)

- Target: `modules/mod-004-network-security/exercise-04-rate-limit-ddos-design/SOLUTION.md`
- Message: modules/mod-004-network-security/exercise-04-rate-limit-ddos-design/SOLUTION.md is missing required heading(s): implementation.
- missing: ['implementation']
- seen: ['1. solution overview', '2. worked design', '2.1 traffic taxonomy', '2.2 l3/l4 edge controls', '2.3 l7 token buckets at the gateway', '2.4 concurrency, queue, and body limits', '2.5 per-workload admission control (inference server)', '2.6 failure modes', '2.7 headers returned to callers', '2.8 observability hooks for exercise 05', '3. validation steps', '3.1 functional checks', '3.2 failure-mode checks (game day)', '3.3 headers and signalling', '4. rubric / review checklist', '5. common mistakes', '6. references', 'solution — exercise 04: rate-limit and ddos design']

### 12. `needs_research_marker` (error)

- Target: `modules/mod-004-network-security/exercise-04-rate-limit-ddos-design/SOLUTION.md`
- Message: modules/mod-004-network-security/exercise-04-rate-limit-ddos-design/SOLUTION.md still contains a `needs-research` marker.

### 13. `missing_required_sections` (error)

- Target: `modules/mod-004-network-security/exercise-05-network-observability-plan/SOLUTION.md`
- Message: modules/mod-004-network-security/exercise-05-network-observability-plan/SOLUTION.md is missing required heading(s): implementation.
- missing: ['implementation']
- seen: ['1. solution overview', '2. worked plan', '2.1 layered telemetry sources', '2.2 schema (canonical fields)', '2.3 pipeline', '2.4 detection set (illustrative)', '2.5 dashboards (grafana / equivalent)', '2.6 retention and access', '2.7 privacy and least-disclosure', '2.8 health of the observability pipeline itself', '3. validation steps', '3.1 telemetry presence', '3.2 synthetic detection inputs', '3.3 privacy review checklist', '3.4 tabletop', '4. rubric / review checklist', '5. common mistakes', '6. references', 'solution — exercise 05: network observability plan']

### 14. `needs_research_marker` (error)

- Target: `modules/mod-004-network-security/exercise-05-network-observability-plan/SOLUTION.md`
- Message: modules/mod-004-network-security/exercise-05-network-observability-plan/SOLUTION.md still contains a `needs-research` marker.

## Output contract

- Edit ONLY the files listed in the findings.
- Preserve the existing content; add or rename headings
  rather than rewriting whole sections.
- Do NOT touch CURRICULUM.md, VERSIONS.md, or anything
  outside the affected files.
