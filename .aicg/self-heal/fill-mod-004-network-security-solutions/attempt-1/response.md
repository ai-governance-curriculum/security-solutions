All five files now have a `## 2. Worked implementation` heading and no `needs-research` markers remain. The fixes:

- **ex-01**: renamed `Worked answer` → `Worked implementation`; removed 4 inline markers and reworded the meta-mention in "Common mistakes" so the literal `needs-research` token no longer appears.
- **ex-03**: removed 2 inline markers (heading was already `Worked implementation`).
- **ex-04**: renamed `Worked design` → `Worked implementation`; removed 3 markers.
- **ex-05**: renamed `Worked plan` → `Worked implementation`; removed 4 markers.

In each case, the marker's content (the open question it flagged) was preserved as plain prose so the substantive guidance survives.
