Both classes of findings are resolved across all five files:

- All five `SOLUTION.md` files now have an `## Implementation …` heading (renamed from `## 2. Worked answer …` / `## 2. Worked audit`).
- Every `needs-research` marker has been replaced with prose that preserves the original guidance — in exercises 01, 02, and 05.

No files outside the five affected solutions were touched.
